# Django settings
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1 [::1]

# Uncomment and set for a cloud deploy -- unset, these all fall back to the
# docker-compose local-dev defaults (sqlite, local disk, insecure dev key).
# SECRET_KEY=
# DEBUG=False
# DATABASE_URL=postgres://user:password@host:5432/dbname
# GS_BUCKET_NAME=